- empty_chart = true
- entity.csv_data.each do |d|
  - d.charts.each do |chart|
    - empty_chart = false
    .row
      .col-12.text-center
        %h2= d.title
        = chart&.to_html
        %p 

- if empty_chart
  グラフ表示する項目はありません。
